<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppBlocker">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppBlocker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- App Activities -->
        <activity android:name=".FullUsageActivity" android:exported="false"/>
        <activity android:name=".FullUsageListActivity" android:exported="false"/>
        <activity android:name=".ManageAppsActivity" android:exported="false"/>
        <activity android:name=".ScheduleSettingsActivity" android:exported="false"/>
        <activity android:name=".DashboardActivity" android:exported="false"/>
        <activity android:name=".SiteBlockerActivity" android:exported="false"/>
        <activity android:name=".PinLockActivity" android:exported="false"/>
        <activity android:name=".PinSettingsActivity" android:exported="false"/>
        <activity android:name=".BiometricAuthActivity" android:exported="false"/>
        <activity android:name=".BiometricSettingsActivity" android:exported="false"/>
        <activity android:name=".BreakModeActivity" android:exported="false"/>


        <!-- Block Screen Activity -->
        <activity
            android:name=".BlockScreenActivity"
            android:theme="@style/Theme.AppBlocker.BlockScreen"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Boot Receiver -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- AppBlocker Accessibility Service -->
        <service
            android:name=".AppBlockerAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>



        <!-- Optional: VPN Web Blocker -->
        <service
            android:name=".vpn.AdultSiteBlockerVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
        </service>

    </application>
</manifest>
